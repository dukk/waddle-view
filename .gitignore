# Has machine specific values for now that are not nessisary for others... WIll fix this later...
waddle-view.*.code-workspace

# Per-machine junction to waddle_display application support (see scripts/link_waddle_display_appdata.*)
.local/waddle-display-appdata/

# Mono-repo — nested Flutter / Dart
.flutter-sdk/

**/.dart_tool/
**/build/
**/coverage/
**/flutter_*.log
**/.packages

# IDE
.idea/
*.iml
.vscode/
*.swp
*.swo

# Env
.env
.env.*
!apps/waddle_display/.env.example
apps/waddle_display/env.development

# Display / operator secrets (never commit)
**/waddle_instance.id
**/waddle_api.key
**/instance.id
*.waddle-backup.zip
**/secret_bundle.bin

# OS
.DS_Store
Thumbs.db

# Generated (when drift/build_runner added later)
*.g.dart
*.freezed.dart
dist/

**/__pycache__/*

# Cursor hook runtime state (QA edit tracking)
.cursor/hooks/state/



deploy/dev-pi/*.tar.gz

**/node_modules/**

# Controller BFF runtime data (SQLite, TLS cache; also excluded in repo-root .dockerignore via **/data)
apps/waddle_controller/data/

*.pem
*.db
