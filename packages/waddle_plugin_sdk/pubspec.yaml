name: waddle_plugin_sdk
description: SDK for Waddle View display plugins (manifest, REST client, sidecar).
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.11.5

dependencies:
  http: ^1.2.2
  shelf: ^1.4.2
  shelf_router: ^1.1.4

dev_dependencies:
  coverage: ^1.15.0
  lints: ^6.0.0
  test: ^1.25.0
