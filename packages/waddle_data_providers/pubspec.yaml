name: waddle_data_providers
description: Concrete data collectors (HTTP → SQLite) for Waddle View and future cloud engines.
publish_to: none
version: 1.0.0
resolution: workspace

environment:
  sdk: ^3.11.5

dependencies:
  waddle_shared:
    path: ../waddle_shared
  drift: ^2.33.0
  http: ^1.2.2
  timezone: ^0.11.0
  webfeed: ^0.7.0
  crypto: ^3.0.6
  meta: ^1.17.0
  icalendar_parser: ^2.1.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
